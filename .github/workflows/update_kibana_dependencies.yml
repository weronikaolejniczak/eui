name: Update Kibana dependencies

on:
  schedule:
    # 03:05 UTC, Monday-Friday
    # "High load times include the start of every hour. If the load is sufficiently high enough,
    # some queued jobs may be dropped. To decrease the chance of delay, schedule your workflow
    # to run at a different time of the hour."
    # Source: https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule
    - cron: '5 3 * * 1-5'
  pull_request_target:
    # `ci:regression-integration-test-kibana` - one-shot on label add.
    # `ci:regression-integration-test-kibana-auto` - on add, then again on every push.
    types: [labeled, synchronize]
  workflow_dispatch:
    inputs:
      release_ref:
        description: Git ref to release
        type: string
        required: true
      pr_title:
        description: Kibana PR title
        default: Update EUI dependencies
        type: string
      pr_body:
        description: Kibana PR body
        default: This PR updates EUI dependencies in Kibana
        type: string
      source_pr_number:
        description: Source EUI PR number. Reuses that PR's Kibana integration branch if a PR is still open.
        type: number
        required: false
concurrency:
  group: ${{ (github.event.pull_request.number || inputs.source_pr_number) && format('kibana-integration-pr-{0}', github.event.pull_request.number || inputs.source_pr_number) || format('kibana-integration-{0}', github.run_id) }}
  cancel-in-progress: ${{ github.event.pull_request.number != '' || inputs.source_pr_number != '' }}
jobs:
  # Triggers the `release.yml` workflow to publish a snapshot.
  # We dispatch (instead of `workflow_call`) so `release.yml` stays the top-level
  # workflow of the publish run and the single npm trusted publishing entry point
  # shared with official releases. `release.yml` kicks off the rest of the
  # Kibana integration chain after publishing.
  trigger_release:
    name: Trigger snapshot release
    runs-on: ubuntu-latest
    if: >-
      (github.event_name != 'pull_request_target' ||
        github.event.label.name == 'ci:regression-integration-test-kibana' ||
        github.event.label.name == 'ci:regression-integration-test-kibana-auto' ||
        (github.event.action == 'synchronize' &&
          contains(github.event.pull_request.labels.*.name, 'ci:regression-integration-test-kibana-auto'))) &&
      (github.event_name != 'schedule' || github.repository == 'elastic/eui')
    permissions:
      actions: write
      contents: read
    steps:
      - name: Dispatch release workflow
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EVENT_NAME: ${{ github.event_name }}
          RELEASE_REF: ${{ github.event_name == 'pull_request_target' && github.event.pull_request.head.sha || inputs.release_ref || github.sha }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_URL: ${{ github.event.pull_request.html_url }}
          INPUT_PR_TITLE: ${{ inputs.pr_title }}
          INPUT_PR_BODY: ${{ inputs.pr_body }}
          INPUT_SOURCE_PR_NUMBER: ${{ inputs.source_pr_number }}
        # language=bash
        run: |
          set -euo pipefail
          workflow_args=(
            "-f" "release_ref=$RELEASE_REF"
            "-f" "type=snapshot"
            "-f" "dry_run=false"
            "-f" "kibana_integration=true"
          )

          if [[ "$EVENT_NAME" == "pull_request_target" ]]; then
            # Triggered by label - use PR context
            workflow_args+=(
              "-f" "kibana_pr_title=[DO NOT MERGE] EUI Regression Integration Test - elastic/eui#$PR_NUMBER"
              "-f" "kibana_pr_body=This PR was created programmatically to run regression integration tests of EUI changes from $PR_URL"
              "-f" "kibana_source_pr_number=$PR_NUMBER"
            )
          elif [[ "$EVENT_NAME" == "schedule" ]]; then
            workflow_args+=(
              "-f" "kibana_pr_title=[DO NOT MERGE][EUI] Nightly Build"
              "-f" "kibana_nightly=true"
            )
          else
            workflow_args+=(
              "-f" "kibana_pr_title=$INPUT_PR_TITLE"
              "-f" "kibana_pr_body=$INPUT_PR_BODY"
            )
            if [[ -n "${INPUT_SOURCE_PR_NUMBER:-}" ]]; then
              workflow_args+=( -f "kibana_source_pr_number=$INPUT_SOURCE_PR_NUMBER" )
            fi
          fi

          # This job has no checkout, so gh cannot infer the repository.
          gh workflow run release.yml --repo "$GITHUB_REPOSITORY" "${workflow_args[@]}"
